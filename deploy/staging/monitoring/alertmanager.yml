route:
  receiver: local-test-only
  group_by: [alertname]
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h
receivers:
  - name: local-test-only
    webhook_configs:
      - url: http://alert-sink:9094/alerts
        send_resolved: true
        max_alerts: 20
